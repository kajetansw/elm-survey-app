{
  "name": "elm-survey-app",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "elm": "elm",
    "elm-format": "elm-format",
    "elm-live": "elm-live",
    "copy-index": "copyfiles index.html dist/",
    "build-elm": "elm make src/Main.elm --output=dist/app.js --optimize",
    "build": "yarn build-elm && yarn copy-index",
    "build-prod": "yarn build-elm && yarn copy-index",
    "format": "elm-format src/",
    "start": "elm-live src/Main.elm -- --output=app.js"
  },
  "author": "Kajetan Swiatek",
  "license": "ISC",
  "dependencies": {
    "elm": "^0.19.1-3"
  },
  "devDependencies": {
    "copyfiles": "^2.4.0",
    "elm-format": "^0.8.4",
    "elm-live": "^4.0.2"
  }
}
